#ifndef __WHATEVER__
#define __WHATEVER__

template <typename T>
void swap(T const & x, T const & y);

template <typename T>
T const & min(T const & x, T const & y);

template <typename T>
T const & max(T const & x, T const & y);

#endif